<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <% if (role == "admin") { %>
                <a class="navbar-brand" href="/admins">PostIt</a>
            <% } %>
            <% if (role == "user") { %>
                <a class="navbar-brand" href="/users">PostIt</a>
            <% } %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
              <li class="nav-item">
                <% if (role == "admin") { %>
                    <a class="nav-link" href="/admins">Home</a>
                <% } %>
                <% if (role == "user") { %>
                    <a class="nav-link" href="/users">Home</a>
                <% } %>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Post</a>
              </li>
              <% if (role == "admin") { %>
                <li class="nav-item">
                  <a class="nav-link" href="#">User List</a>
                </li>
              <% } %>
              <li class="nav-item">
                <a class="nav-link" href="/users/profile">User Profile</a>
              </li>
              <li class="nav-item">
                <a class="btn btn-outline-danger" href="/logout">Logout</a>
              </li>
            </ul>
          </div>
        </div>
    </nav>
</header>